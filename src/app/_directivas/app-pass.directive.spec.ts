import { AppPassDirective } from './app-pass.directive';

describe('AppPassDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPassDirective();
    expect(directive).toBeTruthy();
  });
});
